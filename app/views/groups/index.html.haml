- content_for(:head) do
  = stylesheet_link_tag 'groups'
  = javascript_include_tag 'groups'


= render 'partials/header'

.container#admin_body
  %div#groups
    %div.button
      %button{:id => "add_group"} +
    - @groups.each do |g|
      %div.cell
        %a= g.name


